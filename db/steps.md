To load the JSON with apoc, I needed to setup conf/apoc.conf with
```
apoc.import.file.enabled=true
```

The desktop client keeps the db files under:
```
~/.config/Neo4j\ Desktop/Application/relate-data/dbmss/$ID
```
